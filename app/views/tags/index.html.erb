<div class="container-fluid">
  <div class="row">
  	<% @tags.each do |t| %>
    <div class="col-xs-12 col-md-6 col-lg-4">
      <%= link_to tag_path(t) do %>
        <img src="<%= t.image %>">
        <h2 class="link"><%= t.title %></h2>
      <% end %>
    </div>
    <% end %>
  </div>
</div>
